'use strict';
let exsitlocalstorge=[];
function Trip(name,trip,transport)
{
    this.name=name;
    this.trip=trip;
    this.transport=transport;

}
let from1=document.getElementById('form');
from1.addEventListener('submit',handlesubmit);

function handlesubmit(event)
{
    event.preventDefault();
    l
}